export interface Props {
  query?: string
  onQueryChange: (query?: string) => void
}

function FeedbackTableSearch({ }: Props) {

  return (
    <div>

    </div>
  )
}

export default FeedbackTableSearch
