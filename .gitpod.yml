tasks:
  - name: Setup
    init: pnpm install

github:
  prebuilds:
    branches: true

image:
  file: .gitpod.Dockerfile

vscode:
  extensions:
    - bradlc.vscode-tailwindcss
    - dbaeumer.vscode-eslint
