tasks:
  - name: Setup
    init: yarn install
    command: yarn disable-telemetry

github:
  prebuilds:
    branches: true

image:
  file: .gitpod.Dockerfile

vscode:
  extensions:
    - bradlc.vscode-tailwindcss
